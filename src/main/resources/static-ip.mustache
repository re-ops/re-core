auto lo
iface lo inet loopback

auto eth0
iface eth0 inet static
  address {{ip}}
  netmask {{mask}}
  network {{network}}
  gateway {{gateway}}
  dns-search {{search}}
  dns-nameservers {{#names}}{{.}},{{/names}}
