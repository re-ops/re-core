{
 :operates-on "redis"
 :capistrano {
    :dev {
      :args ["deploy" "-s" "hostname=~{target}"]
      :timeout 300000
      :provided () 
    }
 } 
 
 :name "deploy"
 :src "git://github.com/narkisr/cap-demo.git"
}
